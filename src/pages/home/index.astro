---
import Layout from '../../layouts/Layout.astro';
import heroStyles from './HeroSection.module.css';
import toolsStyles from './ToolsSection.module.css';
import featuresStyles from './FeaturesSection.module.css';
---

<Layout>
	<section class={heroStyles.hero}>
		<div class={heroStyles.heroContent}>
			<h1 class={heroStyles.heroTitle}>
				Manipula tus PDFs
				<span class={heroStyles.gradientText}>sin límites</span>
			</h1>
			<p class={heroStyles.heroDescription}>
				Herramientas gratuitas para convertir, comprimir, dividir y combinar archivos PDF.
				Todo procesado localmente en tu navegador para máxima privacidad.
			</p>
			<div class={heroStyles.heroActions}>
				<a href="#tools" class="btn btn-primary">Explorar herramientas</a>
				<a href="#features" class="btn btn-secondary">Conocer más</a>
			</div>
		</div>
	</section>

	<section id="tools" class={toolsStyles.toolsSection}>
		<div class={toolsStyles.sectionHeader}>
			<h2>Herramientas de conversión y manipulación</h2>
			<p>Todas las herramientas que necesitas para trabajar con PDFs</p>
		</div>
		
		<div class={toolsStyles.toolsGrid}>
			<!-- Conversión a Word -->
			<div class={`${toolsStyles.toolCard} ${toolsStyles.featured}`}>
				<div class={toolsStyles.toolIcon}>
					<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
						<polyline points="14,2 14,8 20,8"></polyline>
						<line x1="16" y1="13" x2="8" y2="13"></line>
						<line x1="16" y1="17" x2="8" y2="17"></line>
					</svg>
				</div>
				<h3>PDF a Word</h3>
				<p>Convierte PDFs a documentos de Word editables</p>
				<span class={toolsStyles.toolBadge}>Más popular</span>
			</div>

			<!-- Conversión a Excel -->
			<div class={toolsStyles.toolCard}>
				<div class={`${toolsStyles.toolIcon} ${toolsStyles.excel}`}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
						<polyline points="14,2 14,8 20,8"></polyline>
						<rect x="8" y="12" width="8" height="7"></rect>
						<line x1="8" y1="16" x2="16" y2="16"></line>
						<line x1="12" y1="12" x2="12" y2="19"></line>
					</svg>
				</div>
				<h3>PDF a Excel</h3>
				<p>Extrae tablas y datos a hojas de cálculo</p>
			</div>

			<!-- Conversión a PowerPoint -->
			<div class={toolsStyles.toolCard}>
				<div class={`${toolsStyles.toolIcon} ${toolsStyles.powerpoint}`}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
						<polyline points="14,2 14,8 20,8"></polyline>
						<rect x="8" y="12" width="8" height="6"></rect>
						<circle cx="10" cy="15" r="1"></circle>
					</svg>
				</div>
				<h3>PDF a PowerPoint</h3>
				<p>Convierte a presentaciones editables</p>
			</div>

			<!-- Conversión a JPG -->
			<div class={toolsStyles.toolCard}>
				<div class={`${toolsStyles.toolIcon} ${toolsStyles.image}`}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
						<circle cx="8.5" cy="8.5" r="1.5"></circle>
						<polyline points="21,15 16,10 5,21"></polyline>
					</svg>
				</div>
				<h3>PDF a JPG</h3>
				<p>Convierte páginas PDF a imágenes</p>
			</div>

			<!-- Conversión a PNG -->
			<div class={toolsStyles.toolCard}>
				<div class={`${toolsStyles.toolIcon} ${toolsStyles.image}`}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
						<circle cx="8.5" cy="8.5" r="1.5"></circle>
						<polyline points="21,15 16,10 5,21"></polyline>
					</svg>
				</div>
				<h3>PDF a PNG</h3>
				<p>Convierte a imágenes con transparencia</p>
			</div>

			<!-- Comprimir -->
			<div class={`${toolsStyles.toolCard} ${toolsStyles.wide}`}>
				<div class={`${toolsStyles.toolIcon} ${toolsStyles.compress}`}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
						<polyline points="7,10 12,15 17,10"></polyline>
						<line x1="12" y1="15" x2="12" y2="3"></line>
					</svg>
				</div>
				<h3>Comprimir PDF</h3>
				<p>Reduce el tamaño de tus PDFs manteniendo la calidad óptima</p>
			</div>

			<!-- Dividir -->
			<div class={toolsStyles.toolCard}>
				<div class={`${toolsStyles.toolIcon} ${toolsStyles.split}`}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
						<polyline points="14,2 14,8 20,8"></polyline>
						<line x1="9" y1="15" x2="15" y2="15"></line>
					</svg>
				</div>
				<h3>Dividir PDF</h3>
				<p>Separa páginas o extrae rangos</p>
			</div>

			<!-- Combinar -->
			<div class={toolsStyles.toolCard}>
				<div class={`${toolsStyles.toolIcon} ${toolsStyles.merge}`}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
						<polyline points="14,2 14,8 20,8"></polyline>
						<line x1="12" y1="18" x2="12" y2="12"></line>
						<line x1="9" y1="15" x2="15" y2="15"></line>
					</svg>
				</div>
				<h3>Combinar PDF</h3>
				<p>Une múltiples PDFs en uno</p>
			</div>

			<!-- Proteger -->
			<div class={toolsStyles.toolCard}>
				<div class={`${toolsStyles.toolIcon} ${toolsStyles.protect}`}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
						<circle cx="12" cy="16" r="1"></circle>
						<path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
					</svg>
				</div>
				<h3>Proteger PDF</h3>
				<p>Añade contraseñas y permisos</p>
			</div>
		</div>
	</section>

	<section id="features" class={featuresStyles.featuresSection}>
		<div class={featuresStyles.sectionHeader}>
			<h2>¿Por qué elegir iHatePDF?</h2>
			<p>Procesamiento local, seguro y sin límites</p>
		</div>
		
		<div class={featuresStyles.featuresGrid}>
			<div class={featuresStyles.featureCard}>
				<div class={featuresStyles.featureIcon}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
					</svg>
				</div>
				<h3>100% Privado</h3>
				<p>Todos los archivos se procesan localmente en tu navegador. Nunca subimos tus documentos a nuestros servidores.</p>
			</div>
			
			<div class={featuresStyles.featureCard}>
				<div class={featuresStyles.featureIcon}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<circle cx="12" cy="12" r="10"></circle>
						<polyline points="12,6 12,12 16,14"></polyline>
					</svg>
				</div>
				<h3>Rápido y Eficiente</h3>
				<p>Procesamiento instantáneo sin esperas. No hay colas ni tiempos de subida lentos.</p>
			</div>
			
			<div class={featuresStyles.featureCard}>
				<div class={featuresStyles.featureIcon}>
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
						<circle cx="8.5" cy="7" r="4"></circle>
						<line x1="20" y1="8" x2="20" y2="14"></line>
						<line x1="23" y1="11" x2="17" y2="11"></line>
					</svg>
				</div>
				<h3>Sin Registro</h3>
				<p>Usa todas las herramientas sin crear cuenta. Acceso inmediato a todas las funcionalidades.</p>
			</div>
		</div>
	</section>
</Layout>